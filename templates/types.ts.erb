<% imports.each do |import_statement| -%>
<%= import_statement %>
<% end -%>
<% if imports.any? -%>

<% end -%>
// Generated by swagger-typescript-api
// Do not edit manually

<% types.each do |type| -%>
<% if type.is_a?(Swagger::Typescript::Api::TypeScript::Types::Interface) && type.name -%>
<% if type.description && !type.description.strip.empty? -%>
/** <%= type.description %> */
<% end -%>
<% if config.use_interface? -%>
<%= export_keyword %><%= type.interface_definition %>
<% else -%>
<%= export_keyword %><%= type.type_definition %>
<% end -%>

<% elsif type.is_a?(Swagger::Typescript::Api::TypeScript::Types::Enum) && type.name -%>
<%= export_keyword %><%= type.enum_definition %>

<% elsif type.is_a?(Swagger::Typescript::Api::TypeScript::Types::Union) && type.name -%>
<%= export_keyword %>type <%= type.name %> = <%= type.types.map(&:to_typescript).join(" | ") %>;

<% elsif type.is_a?(Swagger::Typescript::Api::TypeScript::Types::Intersection) && type.name -%>
<%= export_keyword %>type <%= type.name %> = <%= type.types.map(&:to_typescript).join(" & ") %>;

<% elsif type.is_a?(Swagger::Typescript::Api::TypeScript::Types::Array) && type.name -%>
<%= export_keyword %>type <%= type.name %> = <%= type.to_typescript %>;

<% end -%>
<% end -%>
